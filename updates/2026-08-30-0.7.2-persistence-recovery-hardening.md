# 2026-08-30 - 0.7.2 Persistence and Recovery Hardening

## Perubahan

- Acquisition request menggunakan repository persistence aplikasi dan di-hydrate saat Application initialize.
- Write lifecycle acquisition diserialkan untuk mencegah race create/update pada eksekusi cepat.
- Request identik yang berjalan bersamaan memakai single-flight deduplication.
- Runtime bot meneruskan event death ke EventBus dengan correlation id bot.
- Request acquisition yang belum selesai dipindahkan ke `RECOVERY_REQUIRED` saat requester mati.
- Regression test ditambahkan untuk restart persistence, concurrent deduplication, dan death recovery marker.

## Verifikasi

```text
node --test tests/acquisition.test.js tests/core.test.js tests/bot-runtime.test.js
```

Hasil fokus: 28 test lulus, 0 gagal.

```text
npm test
```

Hasil penuh: 108 test lulus, 0 gagal.

## Batasan

Pembuatan recovery job otomatis dan resume reacquisition setelah item death-drop selesai masih memerlukan evaluator manifest dan assignment recovery bot. Hook saat ini sengaja berhenti pada `RECOVERY_REQUIRED` agar tidak membuat recovery job tanpa manifest valid.
