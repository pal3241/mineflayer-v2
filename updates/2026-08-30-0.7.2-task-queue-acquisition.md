# 2026-08-30 - 0.7.2 Task Queue Acquisition

## Perubahan

- Craft, smelt, dan collect pada `AcquisitionService.acquire()` sekarang dieksekusi melalui GoalService dan TaskExecutor.
- Jalur tersebut memperoleh queue limit per bot, retry, timeout, cancellation, checkpoint, capability validation, dan lifecycle task.
- Storage retrieval tetap memakai transaksi `logistics.retrieve()` karena primitive itu sendiri mengelola reservation, lock, transfer, dan verification atomik.
- Fleet handoff tetap memvalidasi server, dimension, donor count, recipient count, serta hasil drop/pickup.

## Verifikasi

Perintah fokus:

```text
node --test tests/acquisition.test.js tests/ai-coordinator.test.js tests/core.test.js
```

Hasil: 31 test lulus, 0 gagal.
