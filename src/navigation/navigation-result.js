export function navigationResult(session) { return { sessionId: session.id, status: session.status, botId: session.botId, mode: session.mode, target: structuredClone(session.target), startPosition: session.startPosition ? { ...session.startPosition } : null, finalPosition: session.finalPosition ? { ...session.finalPosition } : null, distanceRemaining: session.distanceRemaining, durationMs: session.durationMs, diagnostics: structuredClone(session.diagnostics ?? {}), startedAt: session.startedAt, finishedAt: session.finishedAt, failureCode: session.failureCode, failureMessage: session.failureMessage }; }
